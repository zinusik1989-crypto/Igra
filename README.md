# Колесо удачи — нейрофотосессия

Интерактивная мини-игра «Колесо удачи» для промо-лендинга нейрофотосессии.
Весь код в одном файле — **`index.html`**.

## Как открыть

**Локально:** дважды кликните **`index.html`** — игра откроется в браузере.
Ничего устанавливать не нужно (без Node.js, без сборки).

**По ссылке (GitHub Pages):** после включения Pages игра доступна по адресу
`https://zinusik1989-crypto.github.io/Igra/`

## Возможности

- Колесо из 6 секторов с иконками и подписями
- Плавное вращение с замедлением и определением приза
- Конфетти, свечение, бегущие огоньки, вспышка при выигрыше
- Звуки вращения и выигрыша (кнопка 🔊 для выключения)
- Карточка приза: промокод с копированием, круговой таймер-дедлайн
- Кнопка «Поделиться» (мессенджеры на телефоне / копирование на ПК)
- Адаптив для мобильных и десктопов

## Публикация по ссылке (GitHub Pages)

1. Закоммитьте и запушьте изменения (через GitHub Desktop: **Commit** → **Push**).
2. На github.com откройте репозиторий → **Settings** → **Pages**.
3. В разделе **Source** выберите **Deploy from a branch**, ветка **main**, папка **/(root)** → **Save**.
4. Через 1–2 минуты игра откроется по адресу `https://zinusik1989-crypto.github.io/Igra/`.

В корне репозитория должен быть файл **`.nojekyll`** (уже добавлен) — без него GitHub
показывает текст README вместо игры.

### Если по ссылке виден README, а не колесо

1. Убедитесь, что в репозитории на GitHub есть **`index.html`** и **`.nojekyll`** в корне ветки `main`.
2. **Settings → Pages:** источник — ветка **main**, папка **/ (root)**.
3. Подождите 2–5 минут и обновите страницу с **Ctrl+F5** (сброс кэша).
4. Если в репозитории есть папка **`node_modules`** — удалите её на GitHub (она мешает публикации):
   откройте папку на сайте → выберите файлы → Delete → Commit.

Прямая ссылка на игру: `https://zinusik1989-crypto.github.io/Igra/index.html`

## Настройка

Откройте `index.html` в редакторе:

- **Призы и тексты** — массив `PRIZES` (можно менять названия, описания, цвета, иконки, префиксы промокодов).
- **Время таймера** — переменная `DEADLINE_SEC` (по умолчанию 15 минут).
- **Ссылка для «Поделиться»** — переменная `SHARE_PAGE_URL` (пусто = текущий URL).
- **Действие CTA-кнопки** — обработчик `ctaBtn` (сейчас показывает приз и промокод; подключите свой переход на Telegram / WhatsApp / форму).

## Размещение на сайте

Загрузите `index.html` на любой хостинг или вставьте содержимое в страницу лендинга.
